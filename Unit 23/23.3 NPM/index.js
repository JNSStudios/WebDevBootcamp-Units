// var sillyname = require('sillyname');

import generateStupidName from "sillyname";

var name = generateStupidName();

console.log(`My silly name is ${name}.`);


import { randomSuperhero } from "superheroes";
name = randomSuperhero();

console.log(`Fear not, for ${name} is here!! (cue the theme music)`);
